- 
- the runtime error seems to be because access was denied by the mysql server. 
I added alert for user in the case that a database connection fails and asks them to check the database.properties file 
to make sure the mysql user credentials are correct.

- may you please confirm the mysql server access credentials that you used in the database.properties 
when going through the application set up? 
I noticed the the runtime error noted that the access was denied for user "USERNAME" which was the username that 
I submitted with the database.properties file. This, I note, is different from the username and password left by default in the lab
enviroment provided to me. Just to make sure, I edited the database.properties file so that it has those default credentials from the lab, 
to avoid any misundertanding.


- for the lambda expressions. I believe I clearly stated where they were used.